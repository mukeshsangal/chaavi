import { DatesLabelTimeStamp } from './dates-label-time-stamp';

describe('DatesLabelTimeStamp', () => {
  it('should create an instance', () => {
    expect(new DatesLabelTimeStamp()).toBeTruthy();
  });
});
